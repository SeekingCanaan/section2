spring:
  datasource:
    url: jdbc:postgresql://0.0.0.0:5433/postgres?useSSL=false
    driver-class-name: org.postgresql.Driver
    username: root
    password: root

mybatis:
  type-handlers-package: com.jhy.section2.dao
  mapper-locations: classpath:mapper/*.xml